language: node_js
node_js:
  - 13

services:
  - docker

# Pre-testing installs
install:
  - echo "nothing"

# Scripts à exécuter en guise de tests
before_script:
  - echo "none"

script:
  - docker-compose -f docker-compose-build.yaml build
  - docker tag udagram-api-feed tressisson/udagram-api-feed
  - docker tag udagram-api-user tressisson/udagram-api-user
  - docker tag udagram-reverseproxy tressisson/udagram-reverseproxy
  - docker tag udagram-frontend:local tressisson/udagram-frontend

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
  - docker push tressisson/udagram-api-feed
  - docker push tressisson/udagram-api-user
  - docker push tressisson/udagram-reverseproxy
  - docker push tressisson/udagram-frontend